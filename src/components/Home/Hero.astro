<main class="container">
  <div class="hero">
    <div class="hero_img_cover"></div>
    <article class="hero-content">
      <h2>Hey, I’m Alex Spencer and I love building beautiful websites</h2>
      <a href="#About">
        <div class="hero_link_icon">
          <img
            src="/images/icons/arrow-down.svg"
            alt=""
            aria-hidden="true"
          />
        </div>
        <span>About me</span>
      </a>
    </article>
  </div>
</main>

<style>
  .hero {
    position: relative;
    min-height: 600px;
    margin-top: 55px;
  }

  .hero_img_cover {
    position: absolute;
    width: 100%;
    height: 100%;
    left: 0;
    background-image: url(/images/homepage/desktop/image-homepage-hero.webp);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero-content {
    position: absolute;
    left: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 100%;
    max-width: 445px;
    min-height: 358px;
    background-color: var(--white);
    margin-top: 55px;
    padding-top: 55px;
    padding-right: 50px;
  }

  .hero-content h2 {
    font-size: var(--s-2xl);
    font-family: var(--ibarra);
    font-weight: var(--bold);
    line-height: clamp(40px, 6cqw, 50px);
  }

  .hero-content a {
    display: flex;
    align-items: center;
    max-width: 200px;
    height: 50px;
    background-color: var(--dark-blue);
    color: var(--white);

    &:hover {
      background-color: var(--cyan);
    }

    &:hover .hero_link_icon {
      background-color: var(--dark-cyan);
    }

    &:hover .hero_link_icon img {
      filter: brightness(1.5) saturate(0);
    }
  }

  .hero_link_icon {
    display: grid;
    place-content: center;
    width: 50px;
    height: 100%;
    background-color: var(--dark-blue);
  }

  .hero-content a span {
    font-family: var(--public-sans);
    font-size: 13px;
    letter-spacing: 1px;
    text-transform: uppercase;
    margin-left: 35px;
  }

  @media (width < 1024px) {
    .hero_img_cover {
      background-image: url(/images/homepage/tablet/image-homepage-hero.webp);
    }

    .hero-content {
      max-width: 545px;
      min-height: 300px;
    }
  }

  @media (width < 690px) {
    .hero_img_cover {
      background-image: url(/images/homepage/mobile/image-homepage-hero.webp);
      height: 350px;
    }

    .hero-content {
      max-width: 100%;
      min-height: 253px;
      padding-top: 20px;
    }
  }
</style>
